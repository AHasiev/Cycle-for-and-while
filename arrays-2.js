const arr = ['од', 'дв', 'тр', 'чт', 'пт', 'шс', 'см', 'вс', 'дв', 'дс']
for (let i = 0; i < arr.length; i++) {
    console.log(arr[i])
}

const arr = ['од', 'дв', 'тр', 'чт', 'пт', 'шс', 'см', 'вс', 'дв', 'дс']
let i = 0;
while (i < arr.length){
  console.log(arr[i])
  i++;
}
  